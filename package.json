{
  "name": "get-position",
  "version": "0.1.0",
  "description": "Grabs the position of an element with the option to also include scroll offset.",
  "main": "dist/src/get-position.min.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "node_modules/.bin/webpack --progress --colors --watch",
    "build": "NODE_ENV=production node_modules/.bin/webpack --progress --colors"
  },
  "author": "Brandon Pierce <brandon@brandonjpierce.com> (http://brandonjpierce.com/)",
  "license": "MIT",
  "devDependencies": {
    "babel-core": "^6.18.2",
    "babel-loader": "^6.2.8",
    "node-libs-browser": "^2.0.0",
    "webpack": "^1.9.12"
  }
}
